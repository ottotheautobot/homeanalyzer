# Add ffmpeg to the Railway runtime for the vision pipeline (scene-change
# frame extraction from the bot's mp4). Must include python312 explicitly:
# defining [phases.setup] overrides nixpacks' auto-detected Python provider,
# so omitting it breaks the install step with "python: command not found".

[phases.setup]
nixPkgs = ['python312', 'ffmpeg']
